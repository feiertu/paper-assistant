requests>=2.31.0
pymupdf>=1.24.0
chromadb>=0.5.0,<1.0.0
langchain-text-splitters>=0.3.0
langchain>=0.3.0
voyageai>=0.2.0
# streamlit>=1.30.0  # 已迁移到 Vue 前端，不再需要
fastapi>=0.110.0
uvicorn>=0.27.0
openai>=1.30.0
lxml>=5.0.0
numpy>=1.26.0
pypdf>=4.0.0
alembic>=1.13.0
pydantic>=2.0.0
python-dotenv>=1.0.0
sentence-transformers>=2.2.0
# PDF 解析增强（可选）
# docling>=2.0.0        # IBM Docling: Markdown 转换 + 表格 + 公式 + 阅读顺序
# 混合检索增强（可选）
# rank-bm25>=0.2.2       # BM25 稀疏检索（已内置纯 Python 实现，可选用）
